# Ukázkový lokální config - soubor zkopíruj a přejmenuj na config.local.neon
# Obsahuje konfigurace pro dané lokální prostředí aplikace

parameters:
	database:
		driver: mysqli
		host: localhost
		username:
		password:
		database:
		charset: utf8
		profiler: on

nette:

services:

factories:
